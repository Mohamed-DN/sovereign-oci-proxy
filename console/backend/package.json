{
  "name": "neronet-console-backend",
  "version": "4.0.0",
  "description": "NeroNet Sovereign Mesh Enterprise Management Console Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "migrate": "node -e 'require(\"./db/migrator\").runMigrations(require(\"./db/index\").getDatabase())'",
    "seed": "node -e 'const { getDatabase } = require(\"./db/index\"); const { runMigrations } = require(\"./db/migrator\"); const { seedDatabase } = require(\"./db/seed\"); const db = getDatabase(); runMigrations(db); seedDatabase(db);'",
    "test": "node --test tests/**/*.test.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "better-sqlite3": "^11.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "qrcode": "^1.5.4",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
